doctype html
html(lang='es')
  head
    //if IE
      meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(charset='utf-8')
    title drzewo albums
    link(href='https://fonts.googleapis.com/css?family=Open+Sans', rel='stylesheet')
    link(href='../bootstrap/css/bootstrap.min.css', rel='stylesheet')
    script(src='../bootstrap/js/bootstrap.min.js')

  body
    div.container 
      div.jumbotron
        h3 Elige un álbum para añadir a la colección
          p
          if created
            .alert.alert-success(role='alert')
              | El álbum '#{created}' se ha creado correctamente 
              span.glyphicon.glyphicon.glyphicon-ok(aria-hidden='true')

          if isAlbum 
            h4
              | La carpeta 
              i #{pwd} 
              |  tiene #{totalImages} imágenes, ponle un nombre al álbum para crearlo
            form.form-inline(role='form', data-toggle='validator' method='post')
              .form-group
                label.control-label(for='album') Nombre del álbum:
                input#album.form-control(type='text', required='', name='album')
                button.btn.btn-success(type='submit')
                  span.glyphicon.glyphicon.glyphicon-camera
                  |  Añadir
              .help-block.with-errors
            h4 o sigue navegando por otra carpeta

          .list-group
          a.list-group-item(href='/admin/new?path='+pwd+'../') ..
          each dir in directories
            a.list-group-item(href='/admin/new?path='+pwd+dir+'/') #{dir}

                  
